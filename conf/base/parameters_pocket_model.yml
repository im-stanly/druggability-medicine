# Parameters for the pocket_model pipeline
pocket_model:
  data_dir: data/01_raw/proteins     # directory with protein subdirectories
  model_path: models/pocket_classifier.pkl  # where to save the trained model
  n_points: 2000                     # surface points per protein
